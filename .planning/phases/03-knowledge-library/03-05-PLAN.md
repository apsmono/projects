---
phase: 03-knowledge-library
plan: 05
type: execute
wave: 4
depends_on:
  - 03-04
files_modified:
  - solo-leveling/tests/test_vector_search.py
  - solo-leveling/tests/test_intent_parser.py
  - solo-leveling/tests/test_guide_api.py
  - solo-leveling/tests/test_library_api.py
  - solo-leveling/CHANGELOG.md
  - solo-leveling/AI_CONTEXT.md
autonomous: false
requirements:
  - LIB-01
  - LIB-02
  - LIB-03
  - LIB-05
  - GUIDE-01
  - GUIDE-02
  - GUIDE-03
  - GUIDE-05

must_haves:
  truths:
    - "Full test suite passes offline (unit tests green)"
    - "Integration tests pass when SIGNAL_POSTGRES_DSN_TEST is set and Postgres is running"
    - "FastAPI app starts without errors (lifespan opens pool, registers routers)"
    - "AI Guide panel renders in browser, Command Bar parses intent, Status Banner shows metrics, Distraction Gate captures thoughts"
    - "CHANGELOG.md documents Phase 3 changes"
    - "AI_CONTEXT.md reflects Phase 3 completion and Phase 4 readiness"
  artifacts:
    - path: "solo-leveling/tests/test_vector_search.py"
      provides: "Vector search test coverage"
    - path: "solo-leveling/tests/test_intent_parser.py"
      provides: "Intent parser test coverage"
    - path: "solo-leveling/tests/test_guide_api.py"
      provides: "Guide API test coverage"
    - path: "solo-leveling/CHANGELOG.md"
      provides: "Phase 3 changes documented"
      contains: "Phase 3"
    - path: "solo-leveling/AI_CONTEXT.md"
      provides: "Brain priorities updated"
      contains: "Knowledge Library"
  key_links:
    - from: "tests/"
      to: "all src/vector/, src/core/, src/api/ modules"
      via: "import + mock"
      pattern: "unittest.mock.patch"
---

<objective>
Wave 4: Integration verification, full test suite, documentation updates, and phase close.

Purpose: Validate that all 8 requirements work together end-to-end; update documentation; mark phase complete.
Output: All tests green; CHANGELOG.md and AI_CONTEXT.md updated; phase sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-knowledge-library/03-CONTEXT.md
@.planning/phases/03-knowledge-library/03-RESEARCH.md
@.planning/phases/03-knowledge-library/03-PATTERNS.md
@.planning/phases/03-knowledge-library/03-VALIDATION.md

## Key interfaces from prior plans

From 03-02-PLAN (Wave 1):
- `src/vector/search.py` — `search_library(query, owner_id, mode, limit)`
- `src/api/library.py` — `POST /library/search`, `GET /library/recent`

From 03-03-PLAN (Wave 2):
- `src/core/intent_parser.py` — `parse_intent(text)`
- `src/core/router.py` — `route_command_async(text, source)`
- `src/api/guide.py` — `POST /guide/command`, `GET /guide/status`, `POST /guide/park`

From 03-04-PLAN (Wave 3):
- `dashboard/src/components/guide/AIGuidePanel.tsx` — persistent right-hand panel
- `dashboard/src/components/guide/CommandBar.tsx` — natural language input
- `dashboard/src/components/guide/StatusBanner.tsx` — metrics display
- `dashboard/src/components/guide/DistractionGate.tsx` — quick-capture modal
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full offline test suite</name>
  <files>
    solo-leveling/tests/test_vector_search.py
    solo-leveling/tests/test_intent_parser.py
    solo-leveling/tests/test_guide_api.py
    solo-leveling/tests/test_library_api.py
  </files>
  <action>
    Run the full offline test suite: `cd solo-leveling && python -m unittest discover tests -v`. All unit tests (mocked Postgres, mocked Gemini, mocked Firebase) must pass. Fix any failing tests by adjusting the test mocks or the production code. Do NOT run integration tests (they are gated behind SIGNAL_POSTGRES_DSN_TEST).

    Specifically verify:
    - `tests.test_vector_search` — vector search keyword/vector/hybrid modes, owner scoping, stale embedding handling
    - `tests.test_intent_parser` — LLM parsing, malformed JSON fallback, exception fallback, intent validation
    - `tests.test_guide_api` — /guide/command, /guide/status, /guide/park endpoints
    - `tests.test_library_api` — existing library endpoints still work (regression check)
    - Any other existing test modules that might be affected by router.py changes
  </action>
  <verify>
    <automated>cd solo-leveling && python -m unittest discover tests -v</automated>
  </verify>
  <done>All unit tests in the full suite pass without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Run integration tests with live Postgres</name>
  <files>solo-leveling/tests/test_vector_search.py</files>
  <action>
    Start the Postgres container: `docker compose -f docker-compose.n8n.yml up -d postgres`. Wait for healthcheck. Set SIGNAL_POSTGRES_DSN_TEST to the test DSN. Run integration tests:

    ```bash
    cd solo-leveling
    SIGNAL_POSTGRES_DSN_TEST=postgresql://signal:changeme@localhost:5432/signal \
      python -m unittest tests.test_vector_search.VectorSearchIntegrationTests -v
    ```

    Verify:
    - Vector search returns real results for embedded test documents
    - Hybrid search correctly prefers keyword when results are abundant
    - Owner_id scoping works (entries from other owners are not returned)
    - HNSW index is used (verify with EXPLAIN on the query)

    Stop the container after: `docker compose -f docker-compose.n8n.yml down postgres`.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.n8n.yml up -d postgres && sleep 3 && cd solo-leveling && SIGNAL_POSTGRES_DSN_TEST=postgresql://signal:changeme@localhost:5432/signal python -m unittest tests.test_vector_search.VectorSearchIntegrationTests -v; cd .. && docker compose -f docker-compose.n8n.yml down postgres</automated>
  </verify>
  <done>Integration tests pass with live Postgres; vector search returns real results; hybrid fallback works; owner scoping verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — end-to-end AI Guide flow</name>
  <what-built>Full AI Guide panel with conceptual search, intent parsing, metrics, and distraction parking.</what-built>
  <how-to-verify>
    1. Start the brain: `cd solo-leveling && source .venv/bin/activate && uvicorn src.app:app --port 8000 --reload`
    2. Build and serve the dashboard: `cd dashboard && npx vite build && npx vite preview` (or `python -m http.server 8080`)
    3. Open the dashboard in browser and sign in
    4. Verify the AI Guide panel is visible on the right
    5. Test conceptual search: In CommandBar, type "search for machine learning" and press Enter
       - Verify the response shows library entries related to machine learning (even if no exact keyword match)
    6. Test intent parsing: Type "add a note about quantum computing" and press Enter
       - Verify the intent is parsed as library_capture and a new entry is created
    7. Test status banner: Verify the banner shows entry count and recent captures
    8. Test distraction parking: Click the Park button, type "remember to call mom", click Park it
       - Verify the thought is saved and appears in the library under "thoughts" section
    9. Test Spark Cards: Navigate to Library tab, verify Recent Spark Cards show the 4 most recent entries
    10. Test per-entry Q&A: Open a library entry, verify the AI Q&A panel works (ask a question about the entry)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update CHANGELOG.md and AI_CONTEXT.md</name>
  <files>
    solo-leveling/CHANGELOG.md
    solo-leveling/AI_CONTEXT.md
  </files>
  <action>
    Update `solo-leveling/CHANGELOG.md` with a new section for Phase 3 (Signal — Knowledge Library + Conceptual Search + AI Guide):

    - List added modules:
      - `src/vector/search.py` — semantic search with keyword/vector/hybrid modes
      - `src/core/intent_parser.py` — LLM-driven intent parsing with structured output
      - `src/api/guide.py` — AI Guide REST endpoints (/command, /status, /park)
    - List evolved modules:
      - `src/core/router.py` — added `route_command_async` with LLM intent parsing
      - `src/api/library.py` — added `POST /library/search` and `GET /library/recent`
      - `src/api/v1_router.py` — registered guide router
      - `src/app.py` — `/command` endpoint now uses `route_command_async`
    - List dashboard additions:
      - `dashboard/src/components/guide/AIGuidePanel.tsx` — persistent right-hand panel
      - `dashboard/src/components/guide/CommandBar.tsx` — natural language input
      - `dashboard/src/components/guide/StatusBanner.tsx` — processed-noise metrics
      - `dashboard/src/components/guide/DistractionGate.tsx` — quick-capture modal
      - `dashboard/src/lib/api.ts` — guide API client functions
      - `dashboard/src/hooks/useApi.ts` — guide hooks
    - List config changes:
      - `SIGNAL_INTENT_LLM_MODEL` — model override for intent parsing
      - `SIGNAL_INTENT_KEYWORD_FALLBACK` — enable/disable keyword fallback

    Update `solo-leveling/AI_CONTEXT.md` to reflect Phase 3 completion:
    - Mark Phase 3 as complete (or in-progress if not fully done)
    - Note that the Knowledge Library now has conceptual vector search
    - Note that the AI Guide panel is now available with LLM intent parsing
    - Indicate readiness for Phase 4 (Zen Shell + Clarity Board)
    - Update the "Current Phase" section if applicable
  </action>
  <verify>
    <automated>grep -c "Phase 3" solo-leveling/CHANGELOG.md && grep -c "Knowledge Library" solo-leveling/AI_CONTEXT.md</automated>
  </verify>
  <done>CHANGELOG.md has a Phase 3 section; AI_CONTEXT.md references Knowledge Library + AI Guide as available and Phase 4 ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment -> Production | Tests must not pollute production DB or leak real credentials |
| Manual verification -> Automation | Human verification confirms what automated tests cannot (browser UI behavior) |
| Phase 3 -> Phase 4 | Components must be self-contained for Zen shell migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Information Disclosure | Test logs leak DSN or API key | mitigate | Test env uses fake/test values; real credentials only in .env (gitignored) |
| T-03-20 | Denial of Service | Integration tests leave Postgres running | mitigate | Tests stop container in tearDown or final verify step |
| T-03-21 | Tampering | Dashboard build artifacts leak dev API endpoints | mitigate | Build uses VITE_API_BASE from env; production build points to production API |
</threat_model>

<verification>
- `python -m unittest discover tests -v` passes (offline)
- Integration tests pass with live Postgres (when SIGNAL_POSTGRES_DSN_TEST is set)
- Human confirms AI Guide end-to-end flow works in browser
- CHANGELOG.md and AI_CONTEXT.md updated
</verification>

<success_criteria>
- All unit tests pass offline
- Integration tests pass with live Postgres
- AI Guide panel verified end-to-end in browser (human checkpoint)
- Conceptual search returns semantically related entries
- Intent parsing works via LLM with keyword fallback
- Status banner shows live metrics
- Distraction parking saves thoughts to library
- Recent Spark Cards show most recent entries
- Per-entry Q&A still works (regression check)
- CHANGELOG.md documents Phase 3 changes
- AI_CONTEXT.md reflects Phase 3 completion and Phase 4 readiness
- Phase 3 marked complete in STATE.md
</success_criteria>

<output>
Create `.planning/phases/03-knowledge-library/03-05-SUMMARY.md` when done
</output>
