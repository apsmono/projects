---
phase: 03-knowledge-library
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - solo-leveling/tests/test_vector_search.py
  - solo-leveling/tests/test_intent_parser.py
  - solo-leveling/tests/test_guide_api.py
  - dashboard/src/components/guide/.gitkeep
  - solo-leveling/.env.example
autonomous: true
requirements:
  - LIB-02
  - GUIDE-02
  - GUIDE-03
  - GUIDE-05

must_haves:
  truths:
    - "test_vector_search.py compiles and runs (all tests pass or skip appropriately)"
    - "test_intent_parser.py compiles and runs (all tests pass or skip appropriately)"
    - "test_guide_api.py compiles and runs (all tests pass or skip appropriately)"
    - "Dashboard guide component directory exists"
    - ".env.example contains all new config vars for Phase 3"
  artifacts:
    - path: "solo-leveling/tests/test_vector_search.py"
      provides: "Vector search contract tests"
      exports: ["VectorSearchTests", "VectorSearchIntegrationTests"]
    - path: "solo-leveling/tests/test_intent_parser.py"
      provides: "Intent parser contract tests"
      exports: ["IntentParserTests"]
    - path: "solo-leveling/tests/test_guide_api.py"
      provides: "Guide API contract tests"
      exports: ["GuideAPITests"]
    - path: "dashboard/src/components/guide/.gitkeep"
      provides: "Guide component directory marker"
    - path: "solo-leveling/.env.example"
      provides: "Environment variable documentation"
      contains: "SIGNAL_INTENT_LLM_MODEL"
  key_links:
    - from: "test_vector_search.py"
      to: "src.vector.search (not yet created)"
      via: "import + mock"
      pattern: "patch('src.vector.search.get_pool')"
    - from: "test_intent_parser.py"
      to: "src.core.intent_parser (not yet created)"
      via: "import + mock"
      pattern: "patch('src.core.intent_parser.run_agent')"
    - from: "test_guide_api.py"
      to: "src.api.guide (not yet created)"
      via: "TestClient + mock auth"
      pattern: "patch('src.api.guide.require_auth')"
---

<objective>
Wave 0: Create test stubs and API contract definitions for all Phase 3 modules.

Purpose: Define the contracts that Wave 1-4 implementations must satisfy. Every test file compiles and runs (passing or skipping) before any production code exists. This is the TDD foundation for the phase.
Output: Three test files with contract tests, dashboard component directory, updated .env.example.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-knowledge-library/03-CONTEXT.md
@.planning/phases/03-knowledge-library/03-RESEARCH.md
@.planning/phases/03-knowledge-library/03-PATTERNS.md
@.planning/phases/03-knowledge-library/03-VALIDATION.md

## Key interfaces from RESEARCH.md

Pattern 1 (Hybrid Search):
```python
async def search_library(
    query: str,
    owner_id: str,
    mode: Literal["keyword", "vector", "hybrid"] = "hybrid",
    limit: int = 12,
) -> list[dict[str, Any]]:
```

Pattern 2 (Intent Parser):
```python
async def parse_intent(text: str) -> dict[str, Any]:
    # Returns: {"intent": "library_search", "params": {"query": "..."}, "confidence": 0.95}
```

Pattern 3 (Guide API):
```python
# POST /api/v1/guide/command  {text: "..."}
# GET  /api/v1/guide/status
# POST /api/v1/guide/park     {text: "..."}
```

## Test patterns from Phase 1 (test_vector_foundation.py)

- stdlib `unittest.TestCase`
- `from __future__ import annotations` at line 1
- `from unittest.mock import AsyncMock, MagicMock, patch`
- Async tests wrapped in `asyncio.run()` inside sync test methods
- Integration tests gated behind `@unittest.skipUnless(os.environ.get("SIGNAL_POSTGRES_DSN_TEST"), ...)`
- Env isolation via `patch.dict(os.environ, {...}, clear=True)`
- Mock pool pattern: `mock_pool.connection.return_value.__aenter__ = AsyncMock(return_value=mock_conn)`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_vector_search.py</name>
  <files>solo-leveling/tests/test_vector_search.py</files>
  <action>
    Create `solo-leveling/tests/test_vector_search.py` with contract tests for `src.vector.search`:

    1. File header: `from __future__ import annotations`, `import asyncio`, `import os`, `import unittest`, `from unittest.mock import AsyncMock, MagicMock, patch`
    2. `VectorSearchTests` class (unit tests, always run):
       - `test_search_library_keyword_mode`: patch `src.vector.search._get_store` to return a mock store with `search_entries()`; call `search_library("test", owner_id="owner", mode="keyword")`; assert result equals mock store results
       - `test_search_library_vector_mode`: patch `src.vector.search.get_pool` to return mock connection; patch `src.vector.search.embed_text` to return `[0.1] * 768`; mock `fetchall` to return rows with `entry_id`, `similarity`; assert results include similarity scores
       - `test_search_library_hybrid_prefers_keyword`: mock store returns 6 results (>= limit//2); assert hybrid mode returns keyword results without calling vector search
       - `test_search_library_hybrid_falls_back_to_vector`: mock store returns 1 result (< limit//2); assert hybrid mode calls vector search and augments results
       - `test_search_library_owner_id_scoped`: verify SQL parameters include owner_id in the execute call args
       - `test_search_library_empty_query_returns_empty`: assert empty query returns empty list
    3. `VectorSearchIntegrationTests` class (gated behind `SIGNAL_POSTGRES_DSN_TEST`):
       - `test_live_vector_search_returns_results`: requires live Postgres + Gemini; embed a test document, search for it, assert result contains the document
    4. All tests must import from `src.vector.search` — the module does not exist yet, so tests will fail to import. Use `try/except ImportError` at module level to skip all tests if the module is missing, OR use `unittest.skipIf` with a conditional import.
  </action>
  <verify>
    <automated>cd solo-leveling && python -m py_compile tests/test_vector_search.py</automated>
  </verify>
  <done>test_vector_search.py compiles; has VectorSearchTests with >=5 unit tests and VectorSearchIntegrationTests gated behind SIGNAL_POSTGRES_DSN_TEST.</done>
</task>

<task type="auto">
  <name>Task 2: Create test_intent_parser.py</name>
  <files>solo-leveling/tests/test_intent_parser.py</files>
  <action>
    Create `solo-leveling/tests/test_intent_parser.py` with contract tests for `src.core.intent_parser`:

    1. File header: `from __future__ import annotations`, `import asyncio`, `import json`, `import os`, `import unittest`, `from unittest.mock import patch`
    2. `IntentParserTests` class:
       - `test_parse_intent_returns_structured_dict`: patch `src.core.intent_parser.run_agent` to return valid JSON `{"intent": "library_search", "params": {"query": "ai"}, "confidence": 0.95}`; assert result has keys `intent`, `params`, `confidence`
       - `test_parse_intent_library_search`: mock run_agent returns library_search intent; assert `result["intent"] == "library_search"`
       - `test_parse_intent_park_distraction`: mock run_agent returns park_distraction intent; assert `result["intent"] == "park_distraction"`
       - `test_parse_intent_malformed_json_fallback`: mock run_agent returns invalid JSON; assert fallback to keyword `_detect_intent` (patch `src.core.intent_parser._detect_intent` to return "unknown"); assert confidence is 0.3
       - `test_parse_intent_run_agent_failure_fallback`: mock run_agent raises Exception; assert fallback works and returns valid dict with intent and params
       - `test_parse_intent_preserves_original_text_in_params`: assert fallback includes original text in params
    3. All tests use conditional import or skipIf for `src.core.intent_parser`.
  </action>
  <verify>
    <automated>cd solo-leveling && python -m py_compile tests/test_intent_parser.py</automated>
  </verify>
  <done>test_intent_parser.py compiles; has >=6 unit tests covering LLM parsing, malformed JSON fallback, and exception fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Create test_guide_api.py and scaffold dashboard guide directory</name>
  <files>
    solo-leveling/tests/test_guide_api.py
    dashboard/src/components/guide/.gitkeep
    solo-leveling/.env.example
  </files>
  <action>
    Create three items:

    1. `solo-leveling/tests/test_guide_api.py` with contract tests for `src.api.guide`:
       - File header: `from __future__ import annotations`, `import os`, `import unittest`, `from unittest.mock import patch`
       - `GuideAPITests` class:
         - `test_guide_command_parses_intent`: mock `require_auth` and `parse_intent`; POST to `/api/v1/guide/command` with `{"text": "search library"}`; assert 200 and response contains `intent`
         - `test_guide_command_missing_text_returns_400`: POST with empty body; assert 400
         - `test_guide_status_returns_metrics`: mock `require_auth`; GET `/api/v1/guide/status`; assert 200 and response contains `metrics`
         - `test_guide_park_creates_entry`: mock `require_auth` and library store; POST `/api/v1/guide/park` with `{"text": "stray thought"}`; assert 200 and response contains `entry_id`
         - `test_guide_park_missing_text_returns_400`: POST with empty body; assert 400
       - Use `fastapi.testclient.TestClient` with `src.app:app`
       - All tests use conditional import or skipIf for `src.api.guide`

    2. `dashboard/src/components/guide/.gitkeep` — create the directory and a .gitkeep file so the directory is tracked in git before components are added in Wave 3.

    3. Update `solo-leveling/.env.example` to append Phase 3 config vars:
       ```
       # Signal / AI Guide (Phase 3)
       # Gemini model for intent parsing (default: same as GEMINI_MODEL)
       SIGNAL_INTENT_LLM_MODEL=
       # Enable/disable LLM intent parsing fallback to keyword map
       SIGNAL_INTENT_KEYWORD_FALLBACK=true
       ```
  </action>
  <verify>
    <automated>cd solo-leveling && python -m py_compile tests/test_guide_api.py && test -d ../dashboard/src/components/guide && test -f ../dashboard/src/components/guide/.gitkeep && grep -c "SIGNAL_INTENT_LLM_MODEL" .env.example</automated>
  </verify>
  <done>test_guide_api.py compiles; dashboard/src/components/guide/ directory exists with .gitkeep; .env.example has SIGNAL_INTENT_LLM_MODEL and SIGNAL_INTENT_KEYWORD_FALLBACK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test -> Production code | Tests must not call real LLM APIs or production DBs without explicit gating |
| Mock auth -> Real auth | TestClient must use mocked require_auth, never real Firebase tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Test files import production modules and leak structure | accept | Test files are git-tracked; they only test public interfaces |
| T-03-02 | Denial of Service | Integration tests call real Gemini API without gating | mitigate | All integration tests gated behind SIGNAL_POSTGRES_DSN_TEST; unit tests mock all external calls |
| T-03-03 | Tampering | Test mocks bypass auth checks | accept | Tests run in isolated CI/test environment only |
</threat_model>

<verification>
- `python -m py_compile tests/test_vector_search.py`
- `python -m py_compile tests/test_intent_parser.py`
- `python -m py_compile tests/test_guide_api.py`
- `test -d dashboard/src/components/guide && test -f dashboard/src/components/guide/.gitkeep`
- `grep -c "SIGNAL_INTENT_LLM_MODEL" solo-leveling/.env.example`
</verification>

<success_criteria>
- All three test files compile without syntax errors
- Unit tests skip gracefully when target modules don't exist yet
- Integration tests gated behind SIGNAL_POSTGRES_DSN_TEST
- Dashboard guide component directory created and tracked
- .env.example documents all new Phase 3 config variables
</success_criteria>

<output>
Create `.planning/phases/03-knowledge-library/03-01-SUMMARY.md` when done
</output>
