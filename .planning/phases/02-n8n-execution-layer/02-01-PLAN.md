---
phase: 02-n8n-execution-layer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - solo-leveling/src/n8n/__init__.py
  - solo-leveling/src/n8n/client.py
  - solo-leveling/src/n8n/errors.py
  - solo-leveling/src/core/config.py
  - solo-leveling/tests/test_n8n_execution.py
  - solo-leveling/.env.example
autonomous: true
requirements: [N8N-01, INFRA-05]

must_haves:
  truths:
    - "The brain can send authenticated requests to the n8n REST API"
    - "A forced n8n/backend failure produces a plain-language message with no stack traces"
    - "n8n connection config (base URL, API key) is loaded from environment variables"
  artifacts:
    - path: "solo-leveling/src/n8n/client.py"
      provides: "Authenticated n8n REST API client"
      exports: ["trigger_workflow", "get_execution", "list_executions", "create_credential", "update_credential", "delete_credential", "list_credentials", "health_check"]
    - path: "solo-leveling/src/n8n/errors.py"
      provides: "Error classification and soft message generation"
      exports: ["ErrorClass", "classify_error", "soft_error_message"]
    - path: "solo-leveling/src/n8n/__init__.py"
      provides: "Package init"
    - path: "solo-leveling/tests/test_n8n_execution.py"
      provides: "Test stubs for all n8n modules"
  key_links:
    - from: "solo-leveling/src/n8n/client.py"
      to: "solo-leveling/src/core/config.py"
      via: "N8N_BASE_URL, N8N_API_KEY imports"
      pattern: "from src\\.core\\.config import N8N_BASE_URL, N8N_API_KEY"
    - from: "solo-leveling/src/n8n/errors.py"
      to: "solo-leveling/src/n8n/client.py"
      via: "classify_error wraps client exceptions"
      pattern: "classify_error"
---

<objective>
Build the n8n REST API client and error abstraction layer.

Purpose: The n8n client is the foundation every other n8n module depends on. The error layer ensures owner-facing failures never expose raw stack traces. Together they enable the brain to communicate with n8n safely.

Output: `src/n8n/client.py` (thin httpx client), `src/n8n/errors.py` (error classification), `src/n8n/__init__.py`, config exports, test stubs for all Phase 2 modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-n8n-execution-layer/02-CONTEXT.md
@.planning/phases/02-n8n-execution-layer/02-RESEARCH.md
@.planning/phases/02-n8n-execution-layer/02-PATTERNS.md
@.planning/phases/02-n8n-execution-layer/02-VALIDATION.md

<interfaces>
<!-- Source of truth: the github/client.py pattern to replicate -->

From solo-leveling/src/integrations/github/client.py (the thin-client pattern to follow):
```python
def _headers() -> dict[str, str]:
    return {
        "Authorization": f"Bearer {GITHUB_PAT}",
        "Accept": "application/vnd.github+json",
        ...
    }

def _request(method: str, path: str, json: dict[str, Any] | None = None) -> dict[str, Any]:
    if not GITHUB_PAT:
        raise EnvironmentError("GITHUB_PAT is not set. Add it to your .env file.")
    url = f"{API_BASE}{path}"
    try:
        with httpx.Client(timeout=30.0) as client:
            response = client.request(method, url, headers=_headers(), json=json)
            response.raise_for_status()
            return response.json()
    except httpx.HTTPStatusError as e:
        logger.error("GitHub API error: %s %s — %s", method, path, e.response.text)
        raise
    except Exception:
        logger.exception("GitHub API request failed: %s %s", method, path)
        raise

def health_check() -> dict[str, Any]:
    if not GITHUB_PAT:
        return {"ok": False, "error": "GITHUB_PAT not configured"}
    try:
        r = _request("GET", "/user")
        return {"ok": True, "user": r.get("login", "unknown")}
    except Exception as e:
        return {"ok": False, "error": str(e)}
```

From solo-leveling/src/core/config.py (existing env var pattern):
```python
GITHUB_PAT: str = os.environ.get("GITHUB_PAT", "")
```

From solo-leveling/src/autopilot/governor.py (enum + lookup table pattern):
```python
class ResponsibilityLevel(IntEnum):
    ASSISTED = 1
    ...

_TOOL_RL_REQUIREMENTS: dict[str, ResponsibilityLevel] = {
    "read": ResponsibilityLevel.ASSISTED,
    ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: n8n config exports + error abstraction + package init</name>
  <files>
    solo-leveling/src/core/config.py,
    solo-leveling/src/n8n/__init__.py,
    solo-leveling/src/n8n/errors.py,
    solo-leveling/.env.example
  </files>
  <read_first>
    solo-leveling/src/core/config.py
    solo-leveling/src/autopilot/governor.py
    solo-leveling/.env.example
    solo-leveling/src/integrations/github/__init__.py
  </read_first>
  <behavior>
    - classify_error({"error": "token expired"}) returns ErrorClass.CREDENTIAL_EXPIRED
    - classify_error({"error": "token has been revoked"}) returns ErrorClass.CREDENTIAL_REVOKED
    - classify_error({"error": "invalid_grant"}) returns ErrorClass.CREDENTIAL_EXPIRED
    - classify_error({"error": "ECONNREFUSED"}) returns ErrorClass.N8N_UNREACHABLE
    - classify_error({"error": "workflow not found"}) returns ErrorClass.WORKFLOW_NOT_FOUND
    - classify_error({"error": "429 too many requests"}) returns ErrorClass.RATE_LIMITED
    - classify_error({"error": "something else"}) returns ErrorClass.UNKNOWN
    - soft_error_message(ErrorClass.CREDENTIAL_EXPIRED, integration="gmail") contains "expired" and "reconnect" and no "traceback" or "Exception"
    - soft_error_message(ErrorClass.N8N_UNREACHABLE) contains "isn't responding"
    - soft_error_message(ErrorClass.UNKNOWN) contains "Something went wrong"
    - ErrorClass is a str Enum with exactly 7 members: CREDENTIAL_EXPIRED, CREDENTIAL_REVOKED, INTEGRATION_NOT_CONNECTED, N8N_UNREACHABLE, WORKFLOW_NOT_FOUND, RATE_LIMITED, UNKNOWN
  </behavior>
  <action>
    Add `N8N_BASE_URL` and `N8N_API_KEY` to `solo-leveling/src/core/config.py` after the GitHub section (around line 95). Follow the existing pattern: `N8N_BASE_URL: str = os.environ.get("N8N_BASE_URL", "http://localhost:5678")` and `N8N_API_KEY: str = os.environ.get("N8N_API_KEY", "")`.

    Create `solo-leveling/src/n8n/__init__.py` as an empty file with a one-line docstring: `"""n8n execution engine integration."""`

    Create `solo-leveling/src/n8n/errors.py` per D-09/D-10/D-11 (soft error messages, no stack traces). Use the pattern from `src/autopilot/governor.py` — an `ErrorClass(str, Enum)` with 7 members (CREDENTIAL_EXPIRED, CREDENTIAL_REVOKED, INTEGRATION_NOT_CONNECTED, N8N_UNREACHABLE, WORKFLOW_NOT_FOUND, RATE_LIMITED, UNKNOWN). A `_ERROR_PATTERNS` list of (substring, ErrorClass) tuples for classification. A `_SOFT_MESSAGES` dict mapping ErrorClass to owner-friendly template strings with `{integration}` placeholder. Two public functions: `classify_error(error_data: dict[str, Any]) -> ErrorClass` (string-match against patterns) and `soft_error_message(error_class: ErrorClass, intent: str = "", integration: str = "") -> str` (format template). Per D-11, messages must inform + suggest a likely fix (e.g. "Your Gmail connection may have expired. Would you like to reconnect?"). All files must start with `from __future__ import annotations`. Use absolute imports with `src.` prefix.

    Add `N8N_BASE_URL` and `N8N_API_KEY` to `.env.example` with setup comments explaining that the API key must be generated in the n8n UI (Settings -> API -> Create API Key).
  </action>
  <verify>
    <automated>cd solo-leveling && python -c "from src.n8n.errors import ErrorClass, classify_error, soft_error_message; assert classify_error({'error': 'token expired'}) == ErrorClass.CREDENTIAL_EXPIRED; assert 'expired' in soft_error_message(ErrorClass.CREDENTIAL_EXPIRED, integration='gmail'); print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `solo-leveling/src/core/config.py` contains `N8N_BASE_URL: str = os.environ.get("N8N_BASE_URL"` and `N8N_API_KEY: str = os.environ.get("N8N_API_KEY"`
    - `solo-leveling/src/n8n/__init__.py` exists and contains `"""n8n execution engine integration"""`
    - `solo-leveling/src/n8n/errors.py` contains `class ErrorClass(str, Enum)` with 7 members
    - `solo-leveling/src/n8n/errors.py` contains `def classify_error(error_data: dict[str, Any]) -> ErrorClass:`
    - `solo-leveling/src/n8n/errors.py` contains `def soft_error_message(error_class: ErrorClass`
    - `solo-leveling/.env.example` contains `N8N_BASE_URL=` and `N8N_API_KEY=`
    - No raw exception text or stack traces appear in any `_SOFT_MESSAGES` value
  </acceptance_criteria>
  <done>Config exports, error abstraction module, and package init created. classify_error correctly maps all 7 error patterns. soft_error_message returns owner-friendly text with no stack traces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: n8n REST API client + test stubs</name>
  <files>
    solo-leveling/src/n8n/client.py,
    solo-leveling/tests/test_n8n_execution.py
  </files>
  <read_first>
    solo-leveling/src/integrations/github/client.py
    solo-leveling/src/n8n/errors.py
    solo-leveling/tests/test_autopilot.py
    solo-leveling/src/core/config.py
  </read_first>
  <behavior>
    - client._headers() returns dict with "X-N8N-API-KEY" key
    - client._request("GET", "/workflows", params={"limit": 1}) raises EnvironmentError when N8N_API_KEY is empty
    - client.trigger_workflow(123, data={"key": "val"}) sends POST to /api/v1/workflows/123/run with {"data": {"key": "val"}}
    - client.get_execution(42) sends GET to /api/v1/executions/42
    - client.list_executions(workflow_id=1, status="success", limit=5) sends GET to /api/v1/executions with correct params
    - client.create_credential("test-cred", "gmailOAuth2", {"clientId": "x"}) sends POST to /api/v1/credentials
    - client.update_credential(1, "test-cred", "gmailOAuth2", {"clientId": "x"}) sends PUT to /api/v1/credentials/1
    - client.delete_credential(1) sends DELETE to /api/v1/credentials/1
    - client.list_credentials() sends GET to /api/v1/credentials and returns data list
    - client.health_check() returns {"ok": True} when n8n responds, {"ok": False, "error": "..."} when N8N_API_KEY missing or n8n unreachable
    - httpx.HTTPStatusError is caught and re-raised (propagates to caller)
    - All test stubs in test_n8n_execution.py import correctly (N8NClientTests, CredentialTests, TemplateTests, ExecutorTests, ErrorTests, CallbackTests)
  </behavior>
  <action>
    Create `solo-leveling/src/n8n/client.py` following the `github/client.py` thin-client pattern exactly. Module docstring: `"""n8n Public REST API client.\n\nRequires N8N_BASE_URL and N8N_API_KEY environment variables.\n"""`. Imports: `from __future__ import annotations`, `logging`, `typing.Any`, `httpx`, `from src.core.config import N8N_BASE_URL, N8N_API_KEY`.

    Functions to implement (one per n8n API endpoint):
    - `_headers() -> dict[str, str]` — returns `{"X-N8N-API-KEY": N8N_API_KEY, "Content-Type": "application/json"}`
    - `_request(method, path, json=None, params=None) -> dict[str, Any]` — identical structure to github/client.py `_request` but uses `/api/v1` prefix and 30s timeout. Raises `EnvironmentError` if `N8N_API_KEY` is empty.
    - `trigger_workflow(workflow_id: int | str, data: dict[str, Any] | None = None) -> dict[str, Any]` — POST to `/workflows/{workflow_id}/run` with `{"data": data or {}}`
    - `get_execution(execution_id: int | str) -> dict[str, Any]` — GET to `/executions/{execution_id}`
    - `list_executions(workflow_id=None, status=None, limit=20) -> list[dict[str, Any]]` — GET to `/executions` with optional filters, returns `result.get("data", [])`
    - `create_credential(name, cred_type, data) -> dict[str, Any]` — POST to `/credentials`
    - `update_credential(cred_id, name, cred_type, data) -> dict[str, Any]` — PUT to `/credentials/{cred_id}`
    - `delete_credential(cred_id) -> dict[str, Any]` — DELETE to `/credentials/{cred_id}`
    - `list_credentials() -> list[dict[str, Any]]` — GET to `/credentials`, returns `result.get("data", [])`
    - `health_check() -> dict[str, Any]` — if no API key return error dict, else try `_request("GET", "/workflows", params={"limit": 1})` and return ok/error dict

    Create `solo-leveling/tests/test_n8n_execution.py` with test stubs for ALL six test classes identified in 02-VALIDATION.md: `N8NClientTests`, `CredentialTests`, `TemplateTests`, `ExecutorTests`, `ErrorTests`, `CallbackTests`. Each class should have test method stubs that match the validation map. N8NClientTests and ErrorTests should have GREEN tests (fully implemented). The remaining four classes should have stubs with `self.skipTest("Wave 2")`. Use `unittest.TestCase`, `unittest.mock.patch`, and `from src.app import app` for TestClient. Follow `tests/test_autopilot.py` structure.
  </action>
  <verify>
    <automated>cd solo-leveling && python -m unittest tests.test_n8n_execution -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `solo-leveling/src/n8n/client.py` contains `def _headers() -> dict[str, str]:` returning `X-N8N-API-KEY`
    - `solo-leveling/src/n8n/client.py` contains `def _request(method: str, path: str`
    - `solo-leveling/src/n8n/client.py` contains `def trigger_workflow(workflow_id`
    - `solo-leveling/src/n8n/client.py` contains `def get_execution(execution_id`
    - `solo-leveling/src/n8n/client.py` contains `def list_executions(`
    - `solo-leveling/src/n8n/client.py` contains `def create_credential(`
    - `solo-leveling/src/n8n/client.py` contains `def update_credential(`
    - `solo-leveling/src/n8n/client.py` contains `def delete_credential(`
    - `solo-leveling/src/n8n/client.py` contains `def list_credentials()`
    - `solo-leveling/src/n8n/client.py` contains `def health_check()`
    - `solo-leveling/src/n8n/client.py` contains `raise EnvironmentError("N8N_API_KEY is not set`
    - `solo-leveling/tests/test_n8n_execution.py` contains `class N8NClientTests`
    - `solo-leveling/tests/test_n8n_execution.py` contains `class ErrorTests`
    - `solo-leveling/tests/test_n8n_execution.py` contains `class CredentialTests`
    - `solo-leveling/tests/test_n8n_execution.py` contains `class TemplateTests`
    - `solo-leveling/tests/test_n8n_execution.py` contains `class ExecutorTests`
    - `solo-leveling/tests/test_n8n_execution.py` contains `class CallbackTests`
    - `python -m unittest tests.test_n8n_execution -v` exits 0 (N8NClientTests and ErrorTests green, others skipped)
  </acceptance_criteria>
  <done>n8n REST API client implemented with all endpoint functions. Test file created with 6 test classes — N8NClientTests and ErrorTests fully green, 4 remaining classes stubbed for Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brain -> n8n API | Authenticated API calls carry workflow data and credential tokens |
| env -> brain | N8N_API_KEY loaded from environment; must never leak to logs or error messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | n8n client (API key) | mitigate | API key read from env var only via `src.core.config.N8N_API_KEY`; `_request` raises `EnvironmentError` if missing; `_headers()` includes key in request header only; never logged |
| T-02-02 | Tampering | n8n errors module | mitigate | Error messages are owner-friendly templates only; raw exception text never reaches the AI Guide reply path |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; httpx and pydantic already in requirements.txt |
</threat_model>

<verification>
1. `python -m unittest tests.test_n8n_execution -v` — all N8NClientTests and ErrorTests green
2. `from src.n8n.client import health_check` — imports without error
3. `from src.n8n.errors import ErrorClass, classify_error, soft_error_message` — imports without error
4. `grep -c "N8N_API_KEY" solo-leveling/src/core/config.py` returns 1
</verification>

<success_criteria>
- n8n REST API client follows github/client.py pattern with all 10 endpoint functions
- Error abstraction layer classifies 7 error types and returns plain-language messages
- Config exports N8N_BASE_URL and N8N_API_KEY from env vars
- Test file has 6 test classes; N8NClientTests and ErrorTests are green
- All files use `from __future__ import annotations` and absolute `src.` imports
</success_criteria>

<output>
Create `.planning/phases/02-n8n-execution-layer/02-01-SUMMARY.md` when done
</output>
