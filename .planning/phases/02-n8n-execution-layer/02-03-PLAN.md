---
phase: 02-n8n-execution-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - solo-leveling/src/api/n8n_callback.py
  - solo-leveling/src/api/v1_router.py
  - solo-leveling/tests/test_n8n_execution.py
  - solo-leveling/src/n8n/templates/gmail_read_summary.json
  - solo-leveling/src/n8n/templates/gmail_send_draft.json
  - solo-leveling/CHANGELOG.md
  - solo-leveling/AI_CONTEXT.md
autonomous: false
requirements: [N8N-04, INFRA-05]

must_haves:
  truths:
    - "n8n execution status and webhook callbacks are ingested back into the brain"
    - "Execution results are persisted locally (local-first pattern)"
    - "The callback endpoint is registered on the FastAPI app and reachable at /api/v1/webhook/n8n"
    - "The two starter workflows exist in n8n and their real IDs are recorded in the template JSONs (Wave 0 provisioning), so a live trigger can succeed"
    - "All n8n test classes pass green"
  artifacts:
    - path: "solo-leveling/src/api/n8n_callback.py"
      provides: "POST /webhook/n8n endpoint for n8n execution callbacks"
      exports: ["router"]
    - path: "solo-leveling/src/api/v1_router.py"
      modified: "Added n8n_callback router import and registration"
  key_links:
    - from: "solo-leveling/src/api/n8n_callback.py"
      to: "solo-leveling/src/api/v1_router.py"
      via: "router.include_router(n8n_callback.router)"
      pattern: "n8n_callback\\.router"
    - from: "solo-leveling/src/api/n8n_callback.py"
      to: "data/n8n_executions.json"
      via: "local-first JSON persistence"
      pattern: "n8n_executions\\.json"
---

<objective>
Build the n8n execution callback endpoint and complete all test coverage.

Purpose: n8n workflows POST execution results back to the brain via this callback endpoint (N8N-04). The endpoint persists results locally following the established local-first pattern. This plan also completes all test stubs created in Plan 1 to green.

Output: `src/api/n8n_callback.py` (callback endpoint), v1_router.py updated, all test_n8n_execution.py tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-n8n-execution-layer/02-CONTEXT.md
@.planning/phases/02-n8n-execution-layer/02-RESEARCH.md
@.planning/phases/02-n8n-execution-layer/02-PATTERNS.md
@.planning/phases/02-n8n-execution-layer/02-VALIDATION.md

<interfaces>
<!-- Existing API router pattern to follow -->

From src/api/v1_router.py (existing, to be modified):
```python
from fastapi import APIRouter
from src.api import dashboard, commands, reminders, library, graph, timeline, analysis, planning, autopilot, guide

router = APIRouter(prefix="/api/v1")
router.include_router(dashboard.router)
# ...
router.include_router(guide.router)
```

From src/api/autopilot.py (analog pattern for new endpoint):
```python
from fastapi import APIRouter, Request
router = APIRouter()
@router.post("/...")
async def some_endpoint(request: Request) -> dict[str, str]:
    ...
```

From src/integrations/telegram/webhook.py (JSON persistence pattern):
```python
def _save_processed_id(message_id: str) -> None:
    path = Path("data/telegram_processed.json")
    existing = json.loads(path.read_text()) if path.exists() else []
    existing.append(message_id)
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(existing[-1000:], indent=2))
```

From src/n8n/client.py (Plan 01 — execution polling):
```python
def get_execution(execution_id: int | str) -> dict[str, Any]
```

From src/n8n/executor.py (Plan 02 — orchestrator):
```python
async def execute_intent(intent: str, params: dict[str, Any], owner_id: str = "default-owner") -> dict[str, Any]
```

From src/n8n/errors.py (Plan 01):
```python
def classify_error(error_data: dict[str, Any]) -> ErrorClass
def soft_error_message(error_class: ErrorClass, intent: str = "", integration: str = "") -> str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Webhook callback endpoint + router registration</name>
  <files>
    solo-leveling/src/api/n8n_callback.py,
    solo-leveling/src/api/v1_router.py
  </files>
  <read_first>
    solo-leveling/src/api/autopilot.py
    solo-leveling/src/api/v1_router.py
    solo-leveling/src/integrations/telegram/webhook.py
    solo-leveling/src/n8n/client.py
    solo-leveling/src/app.py
  </read_first>
  <behavior>
    - POST /api/v1/webhook/n8n with valid JSON returns 200 and {"status": "ok"}
    - POST /api/v1/webhook/n8n with invalid JSON returns 200 with {"status": "error", "detail": "Invalid JSON"}
    - POST /api/v1/webhook/n8n with payload {"executionId": "42", "status": "success", "data": {"result": "ok"}} logs the execution to data/n8n_executions.json
    - data/n8n_executions.json contains entries with "execution_id", "status", "data", "timestamp" keys
    - Execution log is capped at 500 entries (rolling window)
    - v1_router.py imports n8n_callback and includes its router
    - Endpoint does NOT require auth (n8n sends the callback, not the owner)
  </behavior>
  <action>
    Create `solo-leveling/src/api/n8n_callback.py` following the `src/api/autopilot.py` router pattern. Module docstring explaining this receives n8n execution completion callbacks.

    Imports: `from __future__ import annotations`, `json`, `logging`, `datetime` (timezone-aware), `pathlib.Path`, `typing.Any`, `fastapi.APIRouter`, `fastapi.Request`.

    `router = APIRouter()`

    `_EXECUTION_LOG_PATH = Path("data/n8n_executions.json")`

    `@router.post("/webhook/n8n")` async endpoint. Receives `Request`, parses JSON. On parse failure: return `{"status": "error", "detail": "Invalid JSON"}`. On success: extract `execution_id`, `status`, `data` from payload. Log via `logger.info("n8n callback: execution=%s status=%s", execution_id, status)`. Call `_log_execution(execution_id, status, data)`. Return `{"status": "ok"}`.

    `_log_execution(execution_id: str, status: str, data: dict[str, Any]) -> None` — load existing JSON array from `_EXECUTION_LOG_PATH` (or empty list), append entry with `execution_id`, `status`, `data`, `timestamp` (UTC ISO format). Cap at 500 entries. Write back. Ensure parent dir exists.

    Update `solo-leveling/src/api/v1_router.py`: add `from src.api import n8n_callback` to the import line (alongside the existing imports). Add `router.include_router(n8n_callback.router)` after the `guide.router` line.
  </action>
  <verify>
    <automated>cd solo-leveling && python -c "
from fastapi.testclient import TestClient
from src.app import app
import json, tempfile, os

client = TestClient(app)

# Test valid callback
response = client.post('/api/v1/webhook/n8n', json={
    'executionId': '42', 'status': 'success', 'data': {'result': 'ok'}
})
assert response.status_code == 200
assert response.json()['status'] == 'ok'
print('PASS: valid callback returns 200')

# Test invalid JSON
response = client.post('/api/v1/webhook/n8n', content='not json',
                       headers={'Content-Type': 'application/json'})
assert response.status_code == 200
assert response.json()['status'] == 'error'
print('PASS: invalid JSON returns error')

# Check execution log was created
from pathlib import Path
log_path = Path('data/n8n_executions.json')
if log_path.exists():
    entries = json.loads(log_path.read_text())
    assert len(entries) >= 1
    assert entries[-1]['execution_id'] == '42'
    print(f'PASS: execution log has {len(entries)} entries')
else:
    print('NOTE: execution log not created (may be in different cwd)')

print('ALL PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - `solo-leveling/src/api/n8n_callback.py` contains `router = APIRouter()`
    - `solo-leveling/src/api/n8n_callback.py` contains `@router.post("/webhook/n8n")`
    - `solo-leveling/src/api/n8n_callback.py` contains `async def n8n_callback(request: Request)`
    - `solo-leveling/src/api/n8n_callback.py` contains `_EXECUTION_LOG_PATH = Path("data/n8n_executions.json")`
    - `solo-leveling/src/api/n8n_callback.py` contains `def _log_execution(`
    - `solo-leveling/src/api/n8n_callback.py` contains `"timestamp"` key in logged entries
    - `solo-leveling/src/api/n8n_callback.py` caps log at 500 entries
    - `solo-leveling/src/api/v1_router.py` contains `from src.api import n8n_callback`
    - `solo-leveling/src/api/v1_router.py` contains `router.include_router(n8n_callback.router)`
    - POST to `/api/v1/webhook/n8n` with valid JSON returns `{"status": "ok"}`
    - POST to `/api/v1/webhook/n8n` with invalid JSON returns `{"status": "error"}`
    - Endpoint does NOT use `Depends(require_auth)` or `Depends(optional_auth)`
  </acceptance_criteria>
  <done>Webhook callback endpoint created and registered on the FastAPI app. Execution results persisted to local JSON store with 500-entry rolling cap. Endpoint reachable at /api/v1/webhook/n8n.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete all test stubs + integration verification</name>
  <files>
    solo-leveling/tests/test_n8n_execution.py,
    solo-leveling/CHANGELOG.md,
    solo-leveling/AI_CONTEXT.md
  </files>
  <read_first>
    solo-leveling/tests/test_n8n_execution.py
    solo-leveling/tests/test_autopilot.py
    solo-leveling/src/n8n/client.py
    solo-leveling/src/n8n/errors.py
    solo-leveling/src/n8n/templates.py
    solo-leveling/src/n8n/credentials.py
    solo-leveling/src/n8n/executor.py
    solo-leveling/src/api/n8n_callback.py
    solo-leveling/src/core/router.py
    solo-leveling/src/api/auth_session.py
    solo-leveling/CHANGELOG.md
    solo-leveling/AI_CONTEXT.md
  </read_first>
  <behavior>
    - All tests in N8NClientTests pass green (trigger_workflow, get_execution, list_executions, create_credential, update_credential, delete_credential, list_credentials, health_check, missing_api_key_raises)
    - All tests in ErrorTests pass green (classify_credential_expired, classify_n8n_unreachable, classify_unknown, soft_message_no_stack_trace, soft_message_includes_integration)
    - All tests in CredentialTests pass green (sync_creates_credential, sync_updates_existing, gmail_token_mapping, github_token_mapping, unknown_integration_raises)
    - All tests in TemplateTests pass green (load_all_templates, match_template_match, match_template_no_match, fill_parameters)
    - All tests in ExecutorTests pass green (execute_intent_readonly, execute_intent_side_effecting_needs_approval_at_rl1, no_template_match_declines, poll_execution_success, retry_once_on_failure, llm_fill_invoked, credential_synced_before_trigger)
    - ExecutorTests.test_execute_intent_side_effecting_needs_approval_at_rl1 patches AUTOPILOT_RL_LEVEL to 1 and does NOT mock the Governor — proving the gate is wired to the owner's real RL (regression guard for the hardcoded-LEAD_EXECUTOR bug)
    - ExecutorTests.test_llm_fill_invoked asserts dispatcher.run_agent is called during parameter fill (D-01)
    - ExecutorTests.test_credential_synced_before_trigger asserts n8n_creds.sync_credential is called before n8n_client.trigger_workflow (N8N-02)
    - All tests in CallbackTests pass green (callback_logs_execution, callback_invalid_json)
    - python -m unittest tests.test_n8n_execution -v exits 0 with all tests passing
  </behavior>
  <action>
    Complete all test stubs in `solo-leveling/tests/test_n8n_execution.py`. Each test class must use `unittest.TestCase` and `unittest.mock.patch` for external dependencies.

    **N8NClientTests** — mock `httpx.Client` using `@patch("src.n8n.client.httpx.Client")`. For each test: set up mock client context manager, mock response with `.json()` and `.raise_for_status()`, call the client function, assert correct URL path and HTTP method. Test `missing_api_key_raises` by patching `N8N_API_KEY` to empty string.

    **ErrorTests** — no mocks needed (pure functions). Test `classify_error` with various error dicts. Test `soft_error_message` returns owner-friendly text. Assert no raw exception text in any message.

    **CredentialTests** — mock `src.n8n.client.create_credential`, `update_credential`, `list_credentials`. Test sync creates when no existing, updates when existing found. Test `_map_token_to_n8n_credential` for gmail and github token formats. Test unknown integration raises ValueError.

    **TemplateTests** — use `tempfile.TemporaryDirectory` to create template JSON files. Test `load_all_templates` reads files. Test `match_template` returns best match. Test `match_template` returns None for unknown. Test `fill_parameters` merges params.

    **ExecutorTests** — mock `n8n_templates.match_template`, `src.n8n.executor._fill_with_llm` (or `dispatcher.run_agent`), `n8n_creds.sync_credential`, `n8n_client.trigger_workflow`, `n8n_client.get_execution`. Tests:
    - `test_execute_intent_readonly` — read-only template (side_effecting=false) skips the RL gate, calls sync_credential then trigger, returns ok.
    - `test_execute_intent_side_effecting_needs_approval_at_rl1` — patch `src.n8n.executor.AUTOPILOT_RL_LEVEL` to `1` and do NOT mock the Governor; assert status is `needs_approval` (regression guard against the hardcoded `ResponsibilityLevel.LEAD_EXECUTOR` bug that auto-approved).
    - `test_no_template_match_declines` — match_template returns None → status `declined`, `_log_unmet_intent` called.
    - `test_poll_execution_success` — get_execution returns terminal success.
    - `test_retry_once_on_failure` — first poll fails, retry path invoked, soft error returned on second failure.
    - `test_llm_fill_invoked` — assert `dispatcher.run_agent` is called during `_fill_with_llm` (D-01).
    - `test_credential_synced_before_trigger` — assert `n8n_creds.sync_credential` is called before `n8n_client.trigger_workflow` (use a shared mock manager / call-order assertion) (N8N-02).

    **CallbackTests** — use `TestClient(app)`. Test valid JSON returns ok. Test invalid JSON returns error. Patch `_EXECUTION_LOG_PATH` to use a temp directory.

    Remove all `self.skipTest("Wave 2")` calls.

    Update `solo-leveling/CHANGELOG.md`: add an "Added" entry at the top of the changelog documenting the n8n execution layer — n8n REST client (`src/n8n/client.py`), credential injection (`src/n8n/credentials.py`) including connect-time sync wiring (`src/api/auth_session.py`), template skeleton matching (`src/n8n/templates.py`), intent executor with LLM parameter fill and RL approval gate (`src/n8n/executor.py`), command-router wiring (`src/core/router.py`), error abstraction (`src/n8n/errors.py`), and webhook callback endpoint (`src/api/n8n_callback.py`). Use the timestamp format `YYYY-MM-DD HH-mm-ss` per solo-leveling CHANGELOG rules.

    Update `solo-leveling/AI_CONTEXT.md` (required by solo-leveling AGENTS.md / CLAUDE.md whenever the current phase or priorities change): record that the n8n Execution Layer (Phase 2) is complete — the brain now triggers n8n workflows, injects owner credentials, ingests callbacks, and softens errors — and note the new `src/n8n/` package and `/api/v1/webhook/n8n` endpoint as active surfaces.
  </action>
  <verify>
    <automated>cd solo-leveling && python -m unittest tests.test_n8n_execution -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `python -m unittest tests.test_n8n_execution -v` exits 0
    - No `self.skipTest` calls remain in the test file
    - N8NClientTests has at least 8 test methods, all passing
    - ErrorTests has at least 4 test methods, all passing
    - CredentialTests has at least 4 test methods, all passing
    - TemplateTests has at least 3 test methods, all passing
    - ExecutorTests has at least 6 test methods including the RL1 needs-approval regression guard, the LLM-fill assertion, and the credential-synced-before-trigger assertion, all passing
    - CallbackTests has at least 2 test methods, all passing
    - `solo-leveling/AI_CONTEXT.md` updated to mark Phase 2 (n8n Execution Layer) complete and list the new `src/n8n/` surfaces
    - `solo-leveling/CHANGELOG.md` has a new dated "Added" entry for the n8n execution layer
    - Full suite passes: `python -m unittest tests.test_stage9_libraries tests.test_integration_smoke tests.test_vector_foundation tests.test_n8n_execution -v` exits 0
  </acceptance_criteria>
  <done>All 6 test classes fully implemented and green. Full test suite passes including existing Phase 1 tests. No skipped tests remain.</done>
</task>

<task type="checkpoint:manual">
  <name>Task 3: Provision starter n8n workflows + record real IDs (Wave 0 live gate)</name>
  <action>
    This is the live-instance gate from 02-VALIDATION.md "Wave 0 Requirements" and "Manual-Only Verifications". The two template JSONs ship with `n8n_workflow_id: 0` placeholders; a live trigger cannot succeed until real workflows exist in n8n and their IDs are recorded. This step is manual because it requires a running n8n instance, a generated API key, and human verification in the n8n UI.

    Steps for the owner/operator:
    1. Start n8n: `docker compose -f docker-compose.n8n.yml up -d` and confirm it is reachable at `http://localhost:5678`.
    2. In the n8n UI, generate an API key (Settings → API → Create API Key) and set `N8N_BASE_URL` + `N8N_API_KEY` in `solo-leveling/.env`.
    3. Create the read-only workflow corresponding to `gmail_read_summary` (a Gmail "get messages" → summarize flow) and the side-effecting workflow corresponding to `gmail_send_draft` (compose → send). Activate both.
    4. Record each workflow's numeric ID into the matching template JSON `n8n_workflow_id` field:
       - `solo-leveling/src/n8n/templates/gmail_read_summary.json`
       - `solo-leveling/src/n8n/templates/gmail_send_draft.json`
    5. Verify connectivity and a live read-only trigger:
       - `cd solo-leveling && python -c "from src.n8n.client import health_check; print(health_check())"` → `{"ok": True, ...}`
       - Trigger the read-only workflow through the brain command path (e.g. `route_command("summarize my gmail")`) and confirm the executor returns a result, the callback logs to `data/n8n_executions.json`, and the n8n UI shows the execution.
       - Sync a Gmail credential via `sync_credential` and confirm the credential appears (and is usable in a node) in the n8n UI.
  </action>
  <acceptance_criteria>
    - Both template JSONs have a non-zero `n8n_workflow_id` matching a real, activated n8n workflow
    - `health_check()` returns `{"ok": True}` against the live n8n instance
    - A live read-only trigger via the brain produces an execution visible in the n8n UI and an entry in `data/n8n_executions.json`
    - A synced Gmail credential is visible/usable in the n8n UI (owner never pasted the raw key)
  </acceptance_criteria>
  <done>The two starter workflows exist and are activated in n8n, their real IDs are recorded in the template JSONs, and a live read-only trigger + credential sync are verified end-to-end through the brain. This closes the Wave 0 live gate so phase success criteria 1–3 are demonstrable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n8n -> brain callback | Callback endpoint receives execution results from n8n (unauthenticated sender) |
| callback -> local filesystem | Execution data persisted to data/n8n_executions.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Spoofing | n8n callback endpoint | accept | Endpoint does not require auth because n8n sends callbacks from Docker internal network. For production hardening, a shared secret header can be added later (like Telegram webhook secret pattern). Personal-first single-tenant deployment mitigates risk. |
| T-02-07 | Denial of Service | execution log bloat | mitigate | Log capped at 500 entries with rolling window. Same pattern used in telegram/webhook.py. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed |
</threat_model>

<verification>
1. `python -m unittest tests.test_n8n_execution -v` — all tests green
2. `python -m unittest tests.test_stage9_libraries tests.test_integration_smoke tests.test_vector_foundation tests.test_n8n_execution -v` — full suite green
3. `curl -X POST http://localhost:8000/api/v1/webhook/n8n -H "Content-Type: application/json" -d '{"executionId":"test","status":"success","data":{}}'` returns 200
4. `grep -c "n8n_callback" solo-leveling/src/api/v1_router.py` returns 2 (import + include_router)
</verification>

<success_criteria>
- n8n callback endpoint registered at /api/v1/webhook/n8n
- Execution results persisted to data/n8n_executions.json with 500-entry cap
- All test_n8n_execution.py tests green (6 classes, ~25 test methods) including the RL1 needs-approval regression guard, LLM-fill, and credential-synced-before-trigger assertions
- Full test suite green including Phase 1 tests
- CHANGELOG.md and AI_CONTEXT.md updated with Phase 2 n8n execution layer changes
- Two starter n8n workflows provisioned and activated, real IDs recorded in the template JSONs, and a live read-only trigger + credential sync verified through the brain (manual checkpoint)
</success_criteria>

<output>
Create `.planning/phases/02-n8n-execution-layer/02-03-SUMMARY.md` when done
</output>
